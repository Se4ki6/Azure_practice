# 本日の目標

1. Bicepで記述した内容と実際の設定内容の意味を1対1で紐づける
2. VMの欄にある各構成の意味を知る

## 1. Bicepで記述した内容と実際の設定内容の意味を1対1で紐づける

### フォルダ構成

>メモ：下記フォルダ構成は`tree /F /A | Set-Clipboard`で出力できた！

```plaintext
C:.
|   main.bicep
|   main.bicepparam
|   main.json
|   
\---modules
        auto_shutdown.bicep
        network.bicep
        virtual_machine.bicep
        
```

#### `modules\`配下の設定を見る

**1. virtual_machine.bicep**

パラメータ：[ask]bicepparamからとってくる？

```bicep
@description('VM 名（CAF 略語 + プレフィックス + 環境）')
param vmName string

@description('デプロイ先リージョン')
param location string

@description('VM サイズ。B系はjapaneastで在庫切れのため2コア汎用を採用')
@allowed([
  'Standard_D2as_v4'
  'Standard_D2s_v3'
])
param vmSize string = 'Standard_D2as_v4'

@description('SSH ログインに使う管理ユーザー名')
@minLength(1)
param adminUsername string

@description('OpenSSH 形式の公開鍵。実デプロイ時に CLI 引数で上書き想定')
param sshPublicKey string

@description('VM を接続する NIC リソース ID')
param nicId string

@description('OS Disk 名（osdisk-{prefix}-{env} 形式）')
param osDiskName string

@description('共通タグ')
param tags object = {}
```

OSを決めていそうなところ

```bicep
// Ubuntu 22.04 LTS Gen2（Canonical 公式）。`latest` でメンテ済みイメージに追従
var imageReference = {
  publisher: 'Canonical'
  offer: '0001-com-ubuntu-server-jammy'
  sku: '22_04-lts-gen2'
  version: 'latest'
}
```

- 確認方法
1. VM 作成画面 →「Basics」タブ →「Image」のドロップダウンで「See all images」をクリック
2. Marketplace 検索で Canonical → Ubuntu Server を選ぶと、右側のプレビューパネルに Offer/SKU 相当の情報が表示される
3. Portal の GUI では publisher/offer/sku を直接テキストで見せてくれないことが多いので、確実に値を確認したいときは Azure CLI が早い:
`az vm image list --publisher Canonical --offer 0001-com-ubuntu-server-jammy --all --output table`

> メモ：Canonicalは Ubuntu Linux を開発・公開している会社

![Portalでの確認画面](https://stpublicdevuhmtgr.blob.core.windows.net/public-assets/images/20260718-001.png)

mainのLinux構成
```bicep
// 最小構成の Linux VM。鍵認証のみ・Boot Diagnostics はマネージドストレージ方式
resource vm 'Microsoft.Compute/virtualMachines@2024-07-01' = {
  name: vmName
  location: location
  tags: tags
  properties: {
    hardwareProfile: {
      vmSize: vmSize
    }
    storageProfile: {
      imageReference: imageReference
      osDisk: {
        name: osDiskName
        createOption: 'FromImage'
        caching: 'ReadWrite'
        diskSizeGB: 30
        managedDisk: {
          storageAccountType: 'Standard_LRS'
        }
      }
    }
    osProfile: {
      computerName: vmName
      adminUsername: adminUsername
      // SSH 鍵認証のみ。adminPassword は意図的に未指定
      linuxConfiguration: {
        disablePasswordAuthentication: true
        ssh: {
          publicKeys: [
            {
              path: '/home/${adminUsername}/.ssh/authorized_keys'
              keyData: sshPublicKey
            }
          ]
        }
        provisionVMAgent: true
      }
    }
    networkProfile: {
      networkInterfaces: [
        {
          id: nicId
          properties: {
            primary: true
          }
        }
      ]
    }
    // Boot Diagnostics をマネージドストレージで有効化（追加 Storage Account 不要）
    diagnosticsProfile: {
      bootDiagnostics: {
        enabled: true
      }
    }
  }
}
```

- ARMのリソースJSONで確認できた
```json
{
    "apiVersion": "2025-04-01",
    "id": "/subscriptions/55b9943a-dd24-4437-a3a6-82bd987e729b/resourceGroups/rg-vm-dev/providers/Microsoft.Compute/virtualMachines/vm-vm-dev",
    "name": "vm-vm-dev",
    "type": "microsoft.compute/virtualmachines",
    "location": "japaneast",
    "tags": {
        "project": "azure-learning",
        "managedBy": "bicep",
        "workload": "vm-minimal",
        "environment": "dev"
    },
    "properties": {
        "hardwareProfile": {
            "vmSize": "Standard_D2as_v4"
        },
        "provisioningState": "Succeeded",
        "vmId": "ebc31760-687e-462d-909f-e29e1635ae11",
        "storageProfile": {
            "imageReference": {
                "publisher": "Canonical",
                "offer": "0001-com-ubuntu-server-jammy",
                "sku": "22_04-lts-gen2",
                "version": "latest",
                "exactVersion": "22.04.202606110"
            },
            "osDisk": {
                "osType": "Linux",
                "name": "osdisk-vm-dev",
                "createOption": "FromImage",
                "caching": "ReadWrite",
                "managedDisk": {
                    "id": "/subscriptions/55b9943a-dd24-4437-a3a6-82bd987e729b/resourceGroups/rg-vm-dev/providers/Microsoft.Compute/disks/osdisk-vm-dev"
                },
                "deleteOption": "Detach"
            },
            "dataDisks": [],
            "diskControllerType": "SCSI"
        },
        "osProfile": {
            "computerName": "vm-vm-dev",
            "linuxConfiguration": {
                "disablePasswordAuthentication": true,
                "ssh": {
                    "publicKeys": [
                        {
                            "path": "/home/azureuser/.ssh/authorized_keys",
                            "keyData": "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAICJE20/UJ+JfMTMSsCfrHa9rVYXwcQiqlN2F8AfzyJHz sksrdik@gmail.com"
                        }
                    ]
                },
                "provisionVMAgent": true,
                "patchSettings": {
                    "patchMode": "ImageDefault",
                    "assessmentMode": "ImageDefault"
                }
            },
            "secrets": [],
            "allowExtensionOperations": true,
            "requireGuestProvisionSignal": true,
            "adminUsername": "azureuser"
        },
        "networkProfile": {
            "networkInterfaces": [
                {
                    "id": "/subscriptions/55b9943a-dd24-4437-a3a6-82bd987e729b/resourceGroups/rg-vm-dev/providers/Microsoft.Network/networkInterfaces/nic-vm-dev",
                    "properties": {
                        "primary": true
                    }
                }
            ]
        },
        "diagnosticsProfile": {
            "bootDiagnostics": {
                "enabled": true
            }
        },
        "timeCreated": "2026-07-11T02:57:50.68Z"
    },
    "etag": "\"4\""
}
```
  - `hardwareProfile.vmSize` → `"Standard_D2as_v4"`
  - `storageProfile.imageReference` → `publisher: "Canonical"` / `offer: "0001-com-ubuntu-server-jammy"` / `sku: "22_04-lts-gen2"` / `version: "latest"`（実際に解決された `exactVersion: "22.04.202606110"` も付与されていた）
  - `storageProfile.osDisk` → `name: "osdisk-vm-dev"` / `caching: "ReadWrite"` / `managedDisk.storageAccountType` は `managedDisk.id`（マネージドディスクリソースへの参照）に変わっていた
  - `osProfile.computerName` → `"vm-vm-dev"`
  - `osProfile.adminUsername` → `"azureuser"`
  - `osProfile.linuxConfiguration.disablePasswordAuthentication` → `true`
  - `osProfile.linuxConfiguration.ssh.publicKeys[0].path` → `"/home/azureuser/.ssh/authorized_keys"`
  - `osProfile.linuxConfiguration.ssh.publicKeys[0].keyData` → 登録したOpenSSH公開鍵の文字列がそのまま入っていた
  - `networkProfile.networkInterfaces[0].id` → `nic-vm-dev` のリソースID（`properties.primary: true`）
  - `diagnosticsProfile.bootDiagnostics.enabled` → `true`


**network.bicep**
```bicep
// =============================================================================
// network.bicep
//   VNet / Subnet / NSG / Public IP / NIC をまとめて作成する最小ネットワーク。
//   VM モジュールに NIC ID を渡すことで疎結合に保つ。
// =============================================================================

@description('リソース名のプレフィックス（小文字正規化済みを推奨）')
param namePrefix string

@description('環境識別子（dev / stg / prod）')
param environment string

@description('デプロイ先リージョン')
param location string

@description('SSH を許可する送信元 IP/CIDR。0.0.0.0/32 は何も許可しないダミー値')
param allowedSshSourceAddressPrefix string

@description('共通タグ')
param tags object = {}

// ---- 命名（CAF 略語 + {prefix}-{env}） ----
var vnetName   = 'vnet-${namePrefix}-${environment}'
var subnetName = 'snet-default'
var nsgName    = 'nsg-${namePrefix}-${environment}'
var pipName    = 'pip-${namePrefix}-${environment}'
var nicName    = 'nic-${namePrefix}-${environment}'

// ---- アドレス空間（最小） ----
var vnetAddressPrefix   = '10.0.0.0/16'
var subnetAddressPrefix = '10.0.0.0/24'

// SSH(22/TCP) を allowedSshSourceAddressPrefix からのみ許可する NSG
// その他の受信は Azure 既定の DenyAllInbound に任せる（明示 Deny は書かない）
resource nsg 'Microsoft.Network/networkSecurityGroups@2024-07-01' = {
  name: nsgName
  location: location
  tags: tags
  properties: {
    securityRules: [
      {
        name: 'Allow-SSH-From-MyIP'
        properties: {
          description: 'SSH を指定送信元 IP のみ許可'
          priority: 1000
          direction: 'Inbound'
          access: 'Allow'
          protocol: 'Tcp'
          sourceAddressPrefix: allowedSshSourceAddressPrefix
          sourcePortRange: '*'
          destinationAddressPrefix: '*'
          destinationPortRange: '22'
        }
      }
    ]
  }
}

// VM を配置するためのプライベートネットワーク本体
resource vnet 'Microsoft.Network/virtualNetworks@2024-07-01' = {
  name: vnetName
  location: location
  tags: tags
  properties: {
    addressSpace: {
      addressPrefixes: [
        vnetAddressPrefix
      ]
    }
  }
}

// VNet 内のセグメント。NIC をここに配置し、NSG をサブネットに紐付ける
resource subnet 'Microsoft.Network/virtualNetworks/subnets@2024-07-01' = {
  parent: vnet
  name: subnetName
  properties: {
    addressPrefix: subnetAddressPrefix
    networkSecurityGroup: {
      id: nsg.id
    }
  }
}

// VM への外部到達点。Standard SKU + Static（Basic SKU は 2025-09-30 でリタイア済）
resource publicIp 'Microsoft.Network/publicIPAddresses@2024-07-01' = {
  name: pipName
  location: location
  tags: tags
  sku: {
    name: 'Standard'
    tier: 'Regional'
  }
  properties: {
    publicIPAllocationMethod: 'Static'
    publicIPAddressVersion: 'IPv4'
  }
}

// VM と VNet/Public IP を接続する NIC（NSG はサブネット側に付与）
resource nic 'Microsoft.Network/networkInterfaces@2024-07-01' = {
  name: nicName
  location: location
  tags: tags
  properties: {
    ipConfigurations: [
      {
        name: 'ipconfig1'
        properties: {
          privateIPAllocationMethod: 'Dynamic'
          subnet: {
            id: subnet.id
          }
          publicIPAddress: {
            id: publicIp.id
          }
        }
      }
    ]
  }
}

// ---- 出力（後続モジュール / 接続情報） ----
@description('VM モジュールに渡す NIC リソース ID')
output nicId string = nic.id

@description('SSH 接続先の Public IP アドレス（Static なので作成時点で確定）')
// 注: Static でも作成直後 / what-if の段階では空文字列で返ることがある（既知の ARM 応答挙動）。
//      実際の IP は `az network public-ip show -g <rg> -n <pipName>` で再確認する。
output publicIpAddress string = publicIp.properties.ipAddress
```

network.bicep で作られる5つのリソース（NSG / VNet / Subnet / Public IP / NIC）は、それぞれ独立したPortalブレードを持つものと、親リソースの中でしか見えないものが混在しているらしい

- NSG
> 何のリソースか：受信/送信トラフィックのフィルタルールの集合。今回は「SSHを指定IPからのみ許可」という1本のルールだけを持つ。
  - Portal検索バーで「ネットワークセキュリティグループ」→ 一覧から nsg-{prefix}-{env}
  - ブレード内 設定 > 受信セキュリティ規則：Allow-SSH-From-MyIP の優先度(1000)・プロトコル(TCP)・送信元・ポート(22)がテーブルで見える
  - 設定 > 有効なセキュリティ規則（Effective security rules）：NSGブレード単体には無く、後述のNICブレードから見る必要がある点に注意

65500:DenyAllInBoundがデフォルトでそれより数値が低ければAllowになりそう

- VNet
> 何のリソースか：VMを配置するプライベートネットワークの箱そのもの（10.0.0.0/16）。
  - Portal検索バーで「仮想ネットワーク」→ vnet-{prefix}-{env}
  - 概要タブでアドレス空間を確認、または 設定 > アドレス空間 ブレードで編集可能な形で確認
  - 設定 > サブネット ブレードに snet-default が一覧表示される（ここがSubnetの入口）

- Subnet
  - VNetブレード → 設定 > サブネット → snet-default をクリックすると詳細画面
  - ここでアドレス範囲(10.0.0.0/24)、そして重要な**「ネットワークセキュリティグループ」欄**に nsg-{prefix}-{env} が紐付いていることが確認できる
    - Bicepコメントの「NSGはサブネット側に付与」を裏付ける画面がここ

- PublicIP
> 何のリソースか：VMへの外部到達点。Standard SKU + Static。
  - Portal検索バーで「パブリックIPアドレス」→ pip-{prefix}-{env}
  -  概要タブでIPアドレスの値、割り当て方法（静的）、SKU（Standard）を確認
  -  Bicepのコメントにある「作成直後は空文字列で返ることがある」はARMのデプロイ出力（what-if/output）の話で、Portal上の値自体はデプロイ完了後なら通常表示されている。もし空に見えたらブレードの更新ボタンを押すか az network public-ip show -g <rg> -n <pipName> --query ipAddress で確認

- NIC(ここに一番情報が集まる)
> 何のリソースか：VMとVNet/Public IPを繋ぐ接続点。
  - Portal検索バーで「ネットワークインターフェイス」→ nic-{prefix}-{env}
  - 概要タブ：紐づくVM名、プライベートIP、パブリックIP、VNet/サブネットが要約表示
  - 設定 > IP構成：ipconfig1 の詳細（動的割り当て、どのサブネットか、どのPublic IPと紐付いているか）
  - 設定 > ネットワークセキュリティグループ：NIC自体にはNSGを付けていないので「サブネットのNSGが適用されている」旨の表示になるはず
  - 設定 > 有効なセキュリティ規則（Effective security rules）：★ここが一番実用的。サブネットNSG + NIC NSG（今回は無し）+ Azure既定ルールをすべて合成した「実際に効いているルール一覧」が出る。「本当にSSHだけ通るか」を確認したいときはここを見るのが一番早い